name: qs_audio_player
description: QS Audio Player
version: 0.0.3
homepage:

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter
  audio_service: ^0.18.3
  audio_session: ^0.1.6+1
  just_audio: ^0.9.18

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
  plugin:
    platforms:
      android:
        package: io.qs.audio_player.qs_audio_player
        pluginClass: QsAudioPlayerPlugin
      ios:
        pluginClass: QsAudioPlayerPlugin
